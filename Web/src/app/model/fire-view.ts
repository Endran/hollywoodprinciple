export class FireView {
  appId: string;
  checked: Map<string, number>;
  clientType: Map<string, number>;
  eventCount: number;
  radio: Map<string, number>;
  sliderAverage: number;
  users: number;
}
